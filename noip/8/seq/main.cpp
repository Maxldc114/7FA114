#include <bits/stdc++.h>
using namespace std;
int main () {
    while (1) {
        system("gen.exe");
        system("ans.exe");
        system("a.exe");
        if (system("fc seq.out seq.ans")) {
            break;
        }
    }
    return 0;
}